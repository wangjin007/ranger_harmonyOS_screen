# 待做

当前版本只镜像画面（USB + 扫码无线）。下面按优先级记录后续要做的事，做完请划掉并在 README 里同步能力说明。

## 功能

- ☐ 反向触控：Mac 点击 / 滑动映射到设备
- ☐ 音频：采集系统声音并在 Mac 播放
- ☐ 键盘输入：Mac 键盘落到设备输入框
- ✓ 旋转：设备横竖屏切换时，Mac 窗口跟着转（不重建采集，避免再次弹出录屏授权）
- ✓ 日常无线扫码：同一 Wi-Fi 下扫 Mac 二维码投屏，不再走 `hdc fport`（首次安装 HAP 仍要 USB / DevEco）
- ☐ 完全免调试投屏：不依赖开发者模式和 Debug 签名
- ☐ 应用商店分发：正式签名、上架（当前仅 Debug 开发包）

## 体验

- ☐ 连接失败时给出更明确的原因（hdc 找不到、录屏被拒、编码没起来、防火墙、配对码）
- ☐ 降低延迟、提高大分辨率下的流畅度
- ✓ USB 拔线 / 换机：断开后刷新设备再连接；无线换机先停当前会话再扫同一张码
- ☐ 记住上次 USB 设备
- ☐ Mac 端自动找到本仓库编出的 HAP，减少手动选包

## 不打算做

- 覆盖旧版安卓底鸿蒙（用 scrcpy）
- 绕过系统录屏授权、采集锁屏 / 密码页 / DRM 画面
